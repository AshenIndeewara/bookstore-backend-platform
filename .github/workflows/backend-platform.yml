name: Build and Deploy Backend Platform

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build config-server
        run: |
          cd config-server
          mvn clean package -DskipTests
          mkdir -p ../build-artifacts
          cp target/config-server-*.jar ../build-artifacts/config-server.jar

      - name: Build eureka-server
        run: |
          cd eureka-server
          mvn clean package -DskipTests
          cp target/eureka-server-*.jar ../build-artifacts/eureka-server.jar

      - name: Build api-gateway
        run: |
          cd api-gateway
          mvn clean package -DskipTests
          cp target/api-gateway-*.jar ../build-artifacts/api-gateway.jar

      - name: Copy ecosystem config
        run: |
          cp deploy/startup-scripts/ecosystem.config.js build-artifacts/

      - name: Authenticate to Google Cloud
        id: 'auth'
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: 'projects/57569495786/locations/global/workloadIdentityPools/github-pool/providers/github-provider'
          service_account: 'bookstore-deployer-sa@bookstore-ashen.iam.gserviceaccount.com'

      - name: Set up gcloud CLI
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: 'bookstore-ashen'

      - name: Upload artifacts to Google Cloud Storage
        run: |
          gsutil cp -r build-artifacts/* gs://bookstore-artifacts/
          echo "Artifacts uploaded to gs://bookstore-artifacts/"

      - name: Trigger VM rolling restart
        run: |
          # Get the instance group URL
          MIG_URL=$(gcloud compute instance-groups managed describe bookstore-platform-mig \
            --zone=${{ secrets.GCP_ZONE }} \
            --format="value(instanceGroup)")

          # Perform a rolling restart
          gcloud compute instance-groups managed rolling-action restart bookstore-platform-mig \
            --zone=${{ secrets.GCP_ZONE }} \
            --max-unavailable=1

          echo "Rolling restart initiated for platform MIG"